name: Shell commands

on: 
  - push
  - push-request

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a string
        run: echo "Hello world"
      - name: Multiline script
        run: |
          node -v
          npm -v